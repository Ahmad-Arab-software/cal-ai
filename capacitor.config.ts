import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'cal-ai',
  webDir: 'dist', // zet hier 'dist' of 'build' afhankelijk van jouw project
};

export default config;
